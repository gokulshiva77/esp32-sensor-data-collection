#ifndef __SPI_SENSOR_H__
#define __SPI_SENSOR_H__

#include "esp_err.h"

// Initialize SPI sensor (stub)
void spi_sensor_init(void);
// Read data from SPI sensor (stub)
int spi_sensor_read(void);
// Deinitialize SPI sensor (stub)
void spi_sensor_deinit(void);

#endif // __SPI_SENSOR_H__
